{
  "name": "postgres-supabase",
  "description": "Postgres/Supabase MCP (SQL read-only örnek)",
  "command": "npx",
  "args": [
    "@modelcontextprotocol/pg-server"
  ],
  "env": {
    "DATABASE_URL": "${PG_DSN}"
  }
}