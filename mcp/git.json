{
  "name": "git-mcp",
  "command": "npx",
  "args": ["@modelcontextprotocol/server-git"],
  "env": { "GITHUB_TOKEN": "YOUR_GH_TOKEN", "REPO_PATH": "${MCP_GIT_REPO_PATH:-.}" },
  "options": { "stdio": "pipe" }
}
